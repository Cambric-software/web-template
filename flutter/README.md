# Flutter integration

This directory is reserved for reusable Flutter-related web components
or future website projects that genuinely require Flutter.

The base website template does not require Flutter to serve the normal
HTML/CSS/JavaScript website.
